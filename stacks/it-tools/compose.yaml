version: "3.3"
services:
  it-tools:
    container_name: it-tools
    image: corentinth/it-tools:latest
    restart: always
    ports:
      - 8099:80
    networks:
      - saffron_default
networks:
  saffron_default:
    external: true
